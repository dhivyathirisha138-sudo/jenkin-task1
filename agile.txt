git init
git status
git add app.py
git commit -m "Initial commit: Added greeting application"
git remote add origin https://github.com/username/simple-git-app.git
git branch -M main
git push -u origin main


git checkout -b feature/add-greeting

git add app.py
git commit -m "Added additional greeting message"

git checkout main
git merge feature/add-greeting
git push origin main


git config --global user.name "Your Name"
git config --global user.email "you@email.com"


git branch
git branch new-branch
git branch -d feature/add-greeting


git clone https://github.com/username/project-name.git



*********
Jenkins CI Pipeline using Git and Maven (Complete Step-by-Step Guide) 
***********



STEP 1: Create a Simple Maven Project (Git Side) 
Project Name: simple-maven-app 
Folder Structure (MANDATORY Maven Standard): 
simple-maven-app/ 
├── pom.xml 
└── src/ 
├── main/java/com/example/App.java 
└── test/java/com/example/AppTest.java 


STEP 2: Create App.java (Main Application) 
File Path: 
src/main/java/com/example/App.java 


Code: 
package com.example; 
public class App { 
public int add(int a, int b) { 
return a + b; 
} 
} 
Explanation: - Defines business logic - add() method returns sum of two numbers 


STEP 3: Create AppTest.java (JUnit Test Case) 
File Path: 
src/test/java/com/example/AppTest.java 
Code: 
 
package com.example; 
 
import org.junit.Test; 
import static org.junit.Assert.*; 
 
public class AppTest { 
 
    @Test 
    public void testAdd() { 
        App app = new App(); 
        assertEquals(5, app.add(2, 3)); 
    } 
} 
 
Explanation: - @Test marks test method - assertEquals compares expected and actual value - Test failure causes build failure 
 
 
STEP 4: Create pom.xml (FULL XML WITH DEPENDENCIES) 
 
File Path: 
simple-maven-app/pom.xml 
 
Code: 
 
<project xmlns="http://maven.apache.org/POM/4.0.0" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd"> 
 
    <modelVersion>4.0.0</modelVersion> 
 
    <groupId>com.example</groupId> 
    <artifactId>simple-maven-app</artifactId> 
    <version>1.0-SNAPSHOT</version> 
 
    <dependencies> 
        <dependency> 
            <groupId>junit</groupId> 
<artifactId>junit</artifactId> 
<version>4.13.2</version> 
<scope>test</scope> 
</dependency> 
</dependencies> 
</project> 


Explanation: - pom.xml defines project configuration - JUnit dependency enables unit testing 


STEP 5: Test Project Locally 
Command: 
mvn clean test 
Expected Output: 
BUILD SUCCESS 


STEP 6: Push Project to GitHub 
Commands: 
git init 
git add . 
git commit -m "Initial Maven project with JUnit test" 
git branch -M main 
git remote add origin <your-repository-url> 
git push -u origin main 
STEP 7: Install Maven on Windows 
1. Download apache-maven-3.9.12-bin.zip 
2. Extract to: 
C:\Program Files\Apache\apache-maven-3.9.12 
3. Set System Variable: 
MAVEN_HOME = C:\Program Files\Apache\apache-maven-3.9.12 
4. Add to PATH: 
   %MAVEN_HOME%\bin 
5. Verify: 
   mvn -version 
 
 
STEP 8: Configure Maven in Jenkins 
 
Path: 
Manage Jenkins → Tools → Maven Installations 
 
Configuration: 
Name: M3 
MAVEN_HOME: C:\Program Files\Apache\apache-maven-3.9.12 
Install Automatically: UNCHECKED 
 
 
STEP 9: Jenkins Pipeline Script (Declarative Pipeline) 
 
pipeline { 
    agent any 
 
    tools { 
        maven 'M3' 
    } 
 
    stages { 
        stage('Checkout Git') { 
            steps { 
                git branch: 'main', 
                    url: '<your-repository-url>' 
            } 
        } 
 
        stage('Build and Test') { 
            steps { 
                bat 'mvn clean test' 
            } 
        } 
    } 
} 
 
STEP 10: Run Jenkins Pipeline 
Steps: 
1. Save pipeline 
2. Click Build Now 
3. Open Console Output 
Expected: - Tests run successfully - BUILD SUCCESS - Finished: SUCCESS 
FINAL RESULT: 
Jenkins checks out the Maven project from GitHub, builds it using Maven, runs JUnit unit 
tests automatically, and reports build success or failure. 
This completes a full CI pipeline.














Pipeline Task 1: 
 
pipeline { 
    agent any 
 
    stages { 
        stage('Checkout Code') { 
            steps { 
                git 'https://github.com/your-username/your-repo.git' 
            } 
        } 
 
        stage('Show Files') { 
            steps { 
                bat 'dir' 
            } 
        } 
    } 
} 
 
Pipeline Task 2: 
 
pipeline { 
    agent any 
 
    stages { 
        stage('Checkout Code') { 
            steps { 
                git 'https://github.com/your-username/your-repo.git' 
            } 
        } 
 
        stage('Print Directory') { 
            steps { 
                bat 'cd' 
            } 
        } 
    } 
} 
Pipeline task 3: 
pipeline { 
    agent any 
 
    stages { 
        stage('Checkout Code') { 
            steps { 
                git 'https://github.com/your-username/your-repo.git' 
            } 
        } 
 
        stage('Print Message') { 
            steps { 
                echo 'Hello! Jenkins Pipeline executed successfully' 
            } 
        } 
    } 
} 
Pipeline task 4:  
pipeline { 
    agent any 
 
    stages { 
        stage('Checkout Code') { 
            steps { 
                git 'https://github.com/your-username/your-repo.git' 
            } 
        } 
 
        stage('Create File') { 
            steps { 
                bat 'echo This file is created by Jenkins > demo.txt' 
            } 
        } 
    } 
} 
Pipeline task 5: 
pipeline { 
    agent any 
 
    stages { 
        stage('Checkout Code') { 
            steps { 
                git 'https://github.com/your-username/your-repo.git' 
            } 
        } 
 
        stage('Read File') { 
            steps { 
                bat 'type README.md' 
            } 
        } 
    } 
}















Jenkins Pipeline - Addition of Two Numbers
(Python + BAT)
Step 1: Python Script (add.py)
import sys
def add_numbers(a, b):
    return a + b
if __name__ == "__main__":
    num1 = int(sys.argv[1])
    num2 = int(sys.argv[2])
    result = add_numbers(num1, num2)
    print("=================================")
    print("Addition Result")
    print("=================================")
    print(f"First Number : {num1}")
    print(f"Second Number: {num2}")
    print(f"Sum          : {result}")
Step 2: Jenkins Pipeline Script
pipeline {
    agent any
    parameters {
        string(name: 'BRANCH_NAME',
               defaultValue: 'main',
               description: 'Git branch')
        string(name: 'NUM1',
               defaultValue: '10',
               description: 'First Number')
        string(name: 'NUM2',
               defaultValue: '20',
               description: 'Second Number')
    }
    stages {
        stage('Checkout') {
            steps {
            }
        }
                git branch: "${params.BRANCH_NAME}",
                    url: 'https://github.com/Ramlingams/pipeline-parameters.git'
        stage('Addition Build') {
            steps {
                bat "python add.py ${params.NUM1} ${params.NUM2}"
            }
        }
    }
}
How to Run
1. Save the pipeline in Jenkins.
2. Click 'Build with Parameters'.
3. Enter NUM1 and NUM2 values.
4. Click Build.
5. Check Console Output to see the addition result











Download Docker, Java (17 to 21), Jenkins LTS
C:\jenkins\
   └── jenkins.war

cd C:\jenkins
java -jar jenkins.war

Installation will happen

After installed
In the command prompt it will show the admin password created copy and store it to login into admin account in Jenkins page.

http://localhost:8080 in browser
 
 

Click install and suggested packages 
It will download and install all required APIs. Make sure the internet is connected

Once everything is over. It will ask to create a admin account and use the already created admin password.
Once everything over. Jenkins is ready for use
Donot close the command prompt window to continue using Jenkins. If close do this again in command prompt. 

 C:\jenkins\
   └── jenkins.war

cd C:\jenkins
java -jar jenkins.war


🔹 STEP 1: Create First Job

Click New Item (left side)

Enter job name:

First-Jenkins-Job


Select:
✅ Freestyle project

Click OK

🔹 STEP 2: Configure the Job

Scroll to Build Steps

Click Add build step

Select:

Execute shell → Linux / macOS
Execute Windows batch command → Windows

Paste this:

echo Jenkins is working successfully
date /t
time /t

Click Save

🔹 STEP 3: Run the Job

Click Build Now

A build number (#1) appears

Click it → Console Output

✅ Expected Output:
Jenkins is working successfully
Fri Dec 26 ...


🎉 This confirms Jenkins can execute jobs.


Develop a python file and push to github account
Create a new job in Jenkins
Go to configure Source Code Management -> Choose GIT and Give the GIT repository URL
Then Branches to build - > change according to branch name */main or */master
Goto Triggers ->Check Poll SCM -> Use cron expression * * * * * or H/1 * * * * *

Then  Goto Build Steps -> command to execute the file (python new.py)
Choose Execute Windows batch command based on windows or Linux or mac

Click Appy and Save

H/1 * * * * *        -> Tracks the git file every 1 min

Click Buildnow
See console output to see the results

After successful output in console. Make changes in the source file in VS Code and push to GITHUB. Check for the next build in Jenkins. Automatically that change will be captured by Jenkins















Jenkin – Docker – Kubernetes (CI-CD Demo)
Step1
Install Docker Desktop
Step2
Enable Kubernetes
1. Open the Docker Dashboard: Launch the Docker Desktop application on your
system.
2. Navigate to Settings: Click the gear icon (Settings or Preferences) in the top-right or
sidebar of the Docker Dashboard.
3. Select the Kubernetes Tab: In the Settings menu, find and select
the Kubernetes tab.
4. Enable Kubernetes: Check the box labeled Enable Kubernetes.
5. Apply & Restart: Click the Apply & Restart button at the bottom of the settings
window. Docker Desktop will then download the necessary Kubernetes components
and start a single-node cluster. This may take a few minutes the first time
Step 3
In Jenkin, enable two plugins (Docker and Docker Pipeline) and Install them
Step 4 - Create Simple Java Application
Create a folder in local machine: simple-java-app
Create Java File App.java
public class App {
public static void main(String[] args) throws Exception {
System.out.println("Java CI/CD Application Started...");
while (true) {
Thread.sleep(5000);
System.out.println("Hello from Java CI/CD Pipeline!");
}
}
}
Create Maven File pom.xml
<project xmlns="http://maven.apache.org/POM/4.0.0"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
http://maven.apache.org/xsd/maven-4.0.0.xsd">
<modelVersion>4.0.0</modelVersion>
<groupId>com.demo</groupId>
<artifactId>simple-java-app</artifactId>
<version>1.0</version>
<build>
<plugins>
<plugin>
<groupId>org.apache.maven.plugins</groupId>
<artifactId>maven-compiler-plugin</artifactId>
<version>3.10.1</version>
<configuration>
<source>17</source>
<target>17</target>
</configuration>
</plugin>
</plugins>
</build>
</project>
Step 5 - Build Locally (Test)
Inside project folder:
mvn clean package
It generates: target/simple-java-app-1.0.jar
Step 6 - Create Dockerfile Dockerfile
FROM eclipse-temurin:17-jdk
WORKDIR /app
COPY target/simple-java-app-1.0.jar app.jar
CMD ["java", "-jar", "app.jar"]
Step 7 - Push Code to GitHub (Before that create a public repository in the Github)
git init
git add .
git commit -m "Initial Java CI/CD commit
git branch -M main
git remote add origin https://github.com/your-reponame/simple-java-app.git
git push -u origin main
Step 8 - Create Kubernetes Deployment File deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
name: java-app
spec:
replicas: 2
selector:
matchLabels:
app: java-app
template:
metadata:
labels:
app: java-app
spec:
containers:
- name: java-app
image: <dockerhub-username>/java-app:latest
---
apiVersion: v1
kind: Service
metadata:
name: java-service
spec:
type: NodePort
selector:
app: java-app
ports:
- port: 80
targetPort: 8080
nodePort: 30008
After creating it, Push it to GitHub:
git add deployment.yaml
git commit -m "Added Kubernetes deployment file"
git push
Step 9 - Configure Jenkins
Run Jenkins
Install Kubernetes CLI plugin
Step 10 - Add DockerHub Credentials
In CI/CD:
1. Jenkins builds Docker image
2. Jenkins pushes image to Docker Hub
3. Kubernetes pulls image from Docker Hub
Without credentials → Push will fail
Steps
Manage Jenkins
↓
Manage Credentials
↓
(System)
↓
Global credentials (unrestricted)
Add Credentials
Fill like this:
Field Value
Kind Username with password
Scope Global
Username your DockerHub username
Password your DockerHub password
ID dockerhub-creds
Description DockerHub Login
Click Create
Step 11 - Create Jenkins Pipeline Job
New Item → Pipeline → Name: Java-CICD
Pipleline Script:
pipeline {
agent any
environment {
DOCKER_IMAGE = " yourdockerhubusername/java-app:latest"
}
stages {
stage('Clone Repository') {
steps {
git branch: 'main',
url: 'https://github.com/your-reponame/simple-java-app.git'
}
}
stage('Build with Maven') {
steps {
bat 'mvn clean package'
}
}
stage('Build Docker Image') {
steps {
bat "docker build -t %DOCKER_IMAGE% ."
}
}
stage('Push to DockerHub') {
steps {
withCredentials([usernamePassword(
credentialsId: 'dockerhub-creds',
usernameVariable: 'DOCKER_USER',
passwordVariable: 'DOCKER_PASS'
)]) {
bat """
docker login -u %DOCKER_USER% -p %DOCKER_PASS%
docker push %DOCKER_IMAGE%
"""
}
}
}
stage('Deploy to Kubernetes') {
steps {
bat 'kubectl apply -f deployment.yaml'
}
}
}
post {
success {
echo 'CI/CD Pipeline executed successfully!'
}
failure {
echo 'Pipeline failed. Please check logs.'
}
}
}
Click → Save → Build Now
Above console output is the success message
Step 12 - Verify Docker and Kubernetes Deployment
Docker Image
Kubernetes
Run this command to verify in command prompt,
kubectl get svc
Summary
CI/CD Flow Explanation
CI
✔ Developer pushes Java code
✔ Jenkins builds using Maven
✔ Docker image created
CD
✔ Image pushed to DockerHub
✔ Kubernetes automatically deploys
✔ Pods running inside cluster
• Jenkins clones repo
• Maven compiles Java
• Docker creates container
• DockerHub stores image
• Kubernetes pulls image
• Pods created & scaled






















Jenkins Pipeline Experiments (Detailed Version) 
Experiment 1:
 Basic Pipeline with Parameter and BAT Command Objective: To verify GitHub integration, 
parameter handling, and execution of simple Windows batch commands using Jenkins Pipeline. 
Version 1 • Task 1: Configure pipeline to checkout source code from GitHub repository. • Task 2: 
Define a string parameter named MESSAGE and print its value in the console output. • Task 3: 
Execute a simple Windows batch command using BAT to display 'Hello from Jenkins'. Version 2 • 
Task 1: Checkout updated code from GitHub after modifying a file in IDE. • Task 2: Execute BAT 
command to display current system date. • Task 3: Print the parameter value again to verify 
parameter passing. Version 3 • Task 1: Checkout latest commit from GitHub. • Task 2: Create a file 
named output.txt using BAT command. • Task 3: Display the contents of output.txt in Jenkins 
console. Version 4 • Task 1: Checkout latest version from GitHub. • Task 2: Accept USERNAME 
parameter from user. • Task 3: Create user.txt file and store the username inside it. 
import java.util.Scanner;
public class GradeAnalyzer {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        System.out.print("Enter the number of students: ");
        int numStudents = input.nextInt();
        double[] grades = new double[numStudents];
        double sum = 0;
        //
 Input Loop
        for (int i = 0; i < numStudents; i++) {
            System.out.print("Enter grade for student " + (i + 1) + ": ");
            grades[i] = input.nextDouble();
            sum += grades[i];
        }
        //
 Calculations
        double average = sum / numStudents;
        double highest = grades[0];
        double lowest = grades[0];
        for (double g : grades) {
            if (g > highest) highest = g;
            if (g < lowest) lowest = g;
        }
        //
 Output Results
        System.out.println("\n--- grade Analysis Results ---");
        System.out.printf("Average Score: %.2f%n", average);
        System.out.println("Highest Score: " + highest);
        System.out.println("Lowest Score:  " + lowest);
        System.out.println("\nGrade Breakdown:");
        for (int i = 0; i < numStudents; i++) {
            System.out.println("Student " + (i + 1) + ": " + grades[i] + " (" + getLetterGrade(grades[i]) +
")");
    }
        }
    // Helper method for grading logic
    public static String getLetterGrade(double score) {
        if (score >= 90) return "A";
        if (score >= 80) return "B";
        if (score >= 70) return "C";
        if (score >= 60) return "D";
        return "F";
    }
}

Version 1
Version 1 • Task 1: Configure pipeline to checkout source code from GitHub repository. • Task 2: 
Define a string parameter named MESSAGE and print its value in the console output. • Task 3: 
Execute a simple Windows batch command using BAT to display 'Hello from Jenkins'. 
pipeline {
    agent any
    parameters {
        string(name: 'MESSAGE', defaultValue: 'Hello from GradeAnalyzer', description: 'User input 
message')
    }
    stages {
        stage('Task 1: Checkout Source Code') {
            steps {
                git
            }
        }
 branch: 'main', url: 'https://github.com/dhivyathirisha138-sudo/studentgrade.git'
        stage('Task 2: Print Parameter') {
            steps {
                //
 Prints the value entered by the user
            }
                echo "Console Output: ${params.MESSAGE}"
        }
        stage('Task 3: Execute BAT Command') {
            steps {
                //
 Execute a simple Windows batch command
                bat
 "echo Hello from Jenkins"
            }
        }
    }
version 2
Version 2 • Task 1: Checkout updated code from GitHub after modifying a file in IDE. • Task 2: 
Execute BAT command to display current system date. • Task 3: Print the parameter value again to 
verify parameter passing. 
pipeline {
    agent any
    parameters {
        string(name: 'MESSAGE', defaultValue: 'Version 2 Running', description: 'Enter message')
    }
    stages {
        stage('Task 1: Checkout Latest Code') {
            steps {
                git
            }
        }
 branch: 'main', url: 'https://github.com/dhivyathirisha138-sudo/studentgrade.git'
        stage('Task 2: System Date') {
            steps {
            }
                bat
 "date /t"
        }
        stage('Task 3: Verify Parameter') {
            steps {
                echo "The current build message is: ${params.MESSAGE}"
                echo "Verify: Code has been updated from IDE."
            }
        }
    }
}
version 3
Version 3 • Task 1: Checkout latest commit from GitHub. • Task 2: Create a file named output.txt 
using BAT command. • Task 3: Display the contents of output.txt in Jenkins console 
pipeline {
    agent any
    stages {
        stage('Task 1: Checkout Latest Commit') {
            steps {
                git
            }
        }
 branch: 'main', url: 'https://github.com/dhivyathirisha138-sudo/studentgrade.git'
        stage('Task 2: Create Output File') {
            steps {
                bat
            }
        }
 "echo Analysis for Student Grade System completed successfully > output.txt"
        stage('Task 3: Display File Content') {
            steps {
                bat
 "type output.txt"
            }
        }
    }
}
version 4
Version 4 • Task 1: Checkout latest version from GitHub. • Task 2: Accept USERNAME parameter 
from user. • Task 3: Create user.txt file and store the username inside it. 
pipeline {
    agent any
    parameters {
        string(name: 'USERNAME', defaultValue: 'DefaultUser', description: 'Enter your name')
    }
    stages {
        stage('Task 1: Checkout') {
            steps {
                git
            }
        }
 branch: 'main', url: 'https://github.com/dhivyathirisha138-sudo/studentgrade.git'
        stage('Task 2 & 3: Save User Info') {
            steps {
                echo "The user running this build is: ${params.USERNAME}"
                bat
                bat
 "echo ${params.USERNAME} > user.txt"
 "type user.txt"
            }
        }
    }
}

Experiment 6: 
Environment Variables 
Objective: To understand usage of environment variables and parameter passing in Jenkins Pipeline.
Version 1 • Task 1: Checkout repository. • Task 2: Print Jenkins environment variables. • Task 3: 
Display custom echo message 
version 1
pipeline {
    agent any
    stages {
        stage('Task 1: Checkout Repository') {
            steps {
                git
            }
 branch: 'main', url: 'https://github.com/dhivyathirisha138-sudo/studentgrade.git'
        }
        stage('Task 2: System Audit (Environment Variables)') {
            steps {
                bat
            }
        }
 "set"
version 2
Version 2
 • Task 1: Checkout repository. 
• Task 2: Define custom environment variable inside pipeline. 
• Task 3: Print environment variable using BAT. 
pipeline {
    agent any
    environment {
        COURSE_NAME = "DevOps_Lab"
        STUDENT_REPO = "StudentGradeProject"
    }
    stages {
        stage('Task 1: Checkout') {
            steps {
                git
            }
 branch: 'main', url: 'https://github.com/dhivyathirisha138-sudo/studentgrade.git'
        }


version 3
Version 3 • Task 1: Checkout repository. • Task 2: Accept VERSION parameter from user. • Task 3: 
Create version.txt file storing version value. 
pipeline {
    agent any
    parameters {
        string(name: 'VERSION', defaultValue: '1.0.0', description: 'Specify the version number')
    }
    stages {
        stage('Task 1: Checkout') {
            steps {
                git
            }
        }
 branch: 'main', url: 'https://github.com/dhivyathirisha138-sudo/studentgrade.git'
        stage('Task 2 & 3: File Versioning') {
            steps {
                echo "User defined version: ${params.VERSION}"
                
                bat "echo ${params.VERSION} > version.txt"
                
                bat "type version.txt"
            }
        }
    }
}



version 4
Version 4 • Task 1: Checkout repository. • Task 2: Print Git commit ID in console. • Task 3: Display 
final build result. 
pipeline {
    agent any
    stages {
        stage('Task 1: Checkout') {
            steps {
                git
 branch: 'main', url: 'https://github.com/dhivyathirisha138-sudo/studentgrade.git'
        }
            }
        stage('Task 2: Print Git Commit ID') {
            steps {
                echo "The unique Git Commit ID for this build is: ${env.GIT_COMMIT}"
            }
        }
stage('Task 3: Final Result') {
            steps {
                echo "Build Status: SUCCESS"
                echo "Build Number: # ${env.BUILD_NUMBER}"
                echo "Project Name: ${env.JOB_NAME}"
            }
        }
    }
